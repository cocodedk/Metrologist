# Concrete UI handoff

Consume the typed engine producer from node 03 and use the scene's typed aligned
gravity and calibration, never its legacy gravity placeholder. One revision-bound
session must control results and export after marks, settings, surface, and image
changes. Check all Mark/Results routes to Retake and every newly accepted image.

The shared marker and dimension validators already exist. SettingsScreen is now
reserved if showing a loaded invalid setting needs changes there. Preserve the
current photo, marks, and surface through settings correction. Pure presenter
helpers may be extracted under `core/measurement`; tests under `core/recovery`.
The required regression class must invoke extracted cases so checks execute them.

Production callback wiring is part of this milestone. Final Android flow tests
belong to the dependent integration task. Document that handoff without claiming
physical-camera verification and without labelling it as an unfinished task here.

Keep the final structured report compact: summary at most 120 words, tests as
short identifiers, and blockers only when truly blocking. Source and tool logs
already preserve implementation detail. Do not repeat the report as long prose.
